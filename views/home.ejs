<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%- include ('navigationBar/navBar') %>
    <div class="main-content">
    <h1><%= title %></h1>
      <% if (user) { %>
        <p>Welcome back, <strong><%= user.fullName || user.email %></strong>!</p>
        <p>You’re signed in. Use the navigation menu to explore mood tracking, resources, and the rest of the CampusWell experience.</p>
      <% } else { %>
        <p>Sign in or create an account to access personalised campus resources, track your wellbeing, and stay connected with the community.</p>
        <div class="cta-row">
          <a class="auth-link auth-link-primary" href="/auth/login">Sign in</a>
          <a class="auth-link" href="/auth/signup">Create account</a>
        </div>
      <% } %>
    </div>
  </body>
</html>
